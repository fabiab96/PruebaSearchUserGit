import { ImgAvatarDirective } from './img-avatar.directive';

describe('ImgAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
